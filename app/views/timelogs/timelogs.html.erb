<div class="jumbotron text-center">
  <h1>Time Log</h1>
</div>

<table class="table">
  <thead>
    <tr>
      <th>Date</th>
      <th>Description</th>
      <th>Time Spent</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @time_logs.each do |log| %>
      <tr>
        <td><%= link_to 'Delete', timelogs_path, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
        <td><%= log.created_at.to_date %></td>
        <td><%= log.description %></td>
        <td><%= log.timespent %> minutes</td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>